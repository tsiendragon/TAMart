schema: 1
event: post:file-edit
capabilities:
  blocking: true
  input_mutability: read-only
  output_mutability: read-only
  exec_model: sync
matcher:
  paths:
    - "backend/app/**"
    - "frontend/lib/**"
action:
  run: scripts/post-write-validate.sh
  timeout: 30
on-unsupported: warn
