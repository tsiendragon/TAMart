schema: 1
event: pre:file-edit
capabilities:
  blocking: true
  input_mutability: read-only
  output_mutability: read-only
  exec_model: sync
matcher:
  paths:
    - "backend/app/**"
    - "frontend/lib/**"
action:
  run: scripts/pre-write-validate.sh
  timeout: 15
on-unsupported: warn
